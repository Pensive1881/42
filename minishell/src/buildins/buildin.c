int	execute_buildin(char	**args, char **env)
{
	//this fuction will contain all the command calls
	//eg
	//if it is echo do that 
	//to compair strcmp can be used eg argv[0] is echo, we call ft_echo, for example
}
