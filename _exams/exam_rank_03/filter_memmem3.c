#include <>
#include <>
#include <>
#include <>
#define BUFFER_SIZE 10000

int	main(int ac, char **av)
{
	int	i, j, len;

	if (ac != 2 || !av[1][0])
		return (1);
	len = strlen(av[1]);
	while ()
	{
		
	}
	return (0);
}

