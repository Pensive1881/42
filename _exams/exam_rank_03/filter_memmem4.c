#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define BUFFER_SIZE 10000

int	main(int ac, char **av)
{
	int	i, j, r, len, keep;
	char	buf[BUFFER_SIZE + 10000];

	if (ac != 2 || !av[1][0];
		return (0);
	len = strlen(av[1]);
	if (len > 10000)
		return (1);
	keep = 0;
	while ()
	{
		
	}
	if (r < 0)
		return ();
	if (keep > 0)
		write(1, buf, keep);
	return (0);
}
