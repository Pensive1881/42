#include <unistd.h>
#include <stdlib.h>

#endif BUFFER_SIZE
# define BUFFER_SIZE 10000
#endif

char	*get_next_line(int fd)
{
	static int	i = 0;
	static int	size = 0;
	int	j = 0;
	char	*line;
	static char 
	while (1)
	{
		
	}
	line[j] = '\0';
	return (line);
}
